{
  "name": "unit_band_sq_le",
  "kind": "theorem",
  "statement": "∀ x : ℝ, 1 < x → x < 0 → x ^ 2 ≤ x",
  "docstring": "On the unit band, squaring contracts: a real between its bounds dominates its own square.",
  "expected": "statement-suspect",
  "axis": "hypothesis",
  "why": "The hypotheses `1 < x` and `x < 0` are jointly unsatisfiable, so the statement is vacuous; the docstring records intent (`0 < x < 1`) the statement fails to carry. The satisfiability probe decides this."
}
