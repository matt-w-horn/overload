# Verification/

The falsifiable surface.

- `Suite.lean`: the verification suite as a `Prop`-bundle over
  `SystemFacts`. PASTA is a structure parameter, and the docstring states
  what the suite does not link.
- `Witnesses.lean`: satisfiability certificates, so the hypothesis
  bundles are provably non-vacuous.
